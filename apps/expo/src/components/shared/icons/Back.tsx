import * as React from "react";
import { Image } from "react-native";

const Back = () => {
  return (
    <Image
      source={require("../../../../assets/icons/back.png")}
      alt="back icon"
    />
  );
};

export default Back;
